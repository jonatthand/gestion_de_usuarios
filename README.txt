estoy trabajando en la Python version 3.13.9

creé un entorno virtual con "venv/Scripts/Activate"
    Luego seleccioné el intérprete con Ctrl+shift+p

congelé la versión con "pip freeze > requirements.txt"
    Para instalarla luego se coloca "pip install -r requirements.txt"

estoy aplicando una arquitectura ordenada en base a la equivalencia del THC de Yari
